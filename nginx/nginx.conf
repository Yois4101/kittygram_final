server {
  listen 80;
  index index.html;

  location /api/ {
    proxy_set_header Host $http_host;
    #proxy_pass http://backend:8008/api/;
  }
  location /admin/ {
    proxy_set_header Host $http_host;
    #proxy_pass http://backend:8008/admin/;
  }
}
